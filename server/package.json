{
  "name": "swifty-companion-token-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Holds the 42 OAuth client secret server-side and hands the app a short-lived access token.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
